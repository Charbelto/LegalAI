langchain-chroma
langchain
langchain-classic
langchain_core
langchain_community
langchain-ollama
langchain-openai
chromadb>=0.5.0
tqdm
requests
pymupdf
ddgs
beautifulsoup4
langgraph>=0.2.0
fastapi
uvicorn[standard]
# Directly imported (backend/models.py, state.py), not just a transitive pull
# from fastapi/langchain - pinning explicitly so a future dependency shuffle
# upstream can't silently drop them.
pydantic
typing_extensions
scipy
statsmodels
pytest
pandas
numpy
matplotlib
seaborn

# Local PEFT generation and QLoRA fine-tuning (torch, transformers, peft, trl,
# bitsandbytes) live in requirements-finetune.txt - ~3GB of wheels, and torch
# must come from PyTorch's own index to get a CUDA build. Everything in this
# file works without them.

